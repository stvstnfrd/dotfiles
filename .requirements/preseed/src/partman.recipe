256 32000 512 ext4
    $primary{ } $bootable{ }
    method{ format } format{ }
    use_filesystem{ } filesystem{ ext4 }
    mountpoint{ /boot }

16384 16386 -1 ext4
    method{ format } format{ }
    use_filesystem{ } filesystem{ ext4 }
    mountpoint{ / }

8192 8241 16384 ext4
    method{ format } format{ }
    use_filesystem{ } filesystem{ ext4 }
    mountpoint{ /home }

8192 8241 300% linux-swap
    $primary{ }
    method{ swap } format{ }
